[package]
name = "utils-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.219", features = ["derive"], optional = true }
thiserror = "2.0.16"

[features]
default = ["serde"]
serde = ["dep:serde"]
